function flag = validateMSS_foo(X, s)

% flag = validateMSS_foo(X, s)
%
% DESC:
% Validates the MSS obtained via the sampling of the data before computing
% the parameter vector Theta
%
% INPUT:
% X                 = samples on the manifold
% s                 = indices of the MSS
%
% OUTPUT:
% flag              = true if the MSS is valid

% perform here the check on the MSS

return;
